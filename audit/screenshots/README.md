# Screenshots Directory

This folder is reserved for responsive audit screenshots (`route_width.png` style naming).

For this run, the primary machine-readable outputs were generated into:
- `audit/raw/responsive-matrix.json`
- `audit/raw/responsive-summary.md`

If you want image artifacts on the next pass, the matrix runner can be configured to persist every viewport capture into this directory.

